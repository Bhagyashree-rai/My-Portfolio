function downloadCV(){
        // // Create a link element
        // var downloadLink = document.createElement('a');
        // // Set the href attribute to the path of your resume file
        // downloadLink.href = './assets/Bhagyashree_Resume(1).pdf';
        // // Set the download attribute to specify the filename to be downloaded
        // downloadLink.download = 'Bhagyashree_Resume.pdf';
        // // Append the link to the body
        // document.body.appendChild(downloadLink);
        // // Programmatically trigger the click event of the link
        // downloadLink.click();
        // // Remove the link from the body
        // document.body.removeChild(downloadLink);
        document.getElementById('downloadLink').click();
;}